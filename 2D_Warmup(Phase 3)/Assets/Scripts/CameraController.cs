using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraController : MonoBehaviour
{
    //[SerializeField] private Transform player;
    //private void Update()
    //{
    //    transform.position = new Vector3(player.position.x,player.position.y, transform.position.z); //transform.position.z same as we set it in the editor
    //        //transform in lowercase refer of the same object this component is in.(Transform attribute for the main camera)
    //}
}
